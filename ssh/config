Host *
    # Use ControlMaster/ControlPath to reuse connections
    ControlMaster auto
    ControlPath ~/.ssh/control-%h-%p-%r
    # Forward ssh agent to reuse in-memory keys
    ForwardAgent yes
    Compression yes

Include ./home_config
Include ./bastion_config
